# ==============================================================================
# Indian Legal Reasoning Model (Omilos Own AI) - Dependencies
# ==============================================================================

# Phase 1 & 2: Infrastructure & Tokenizer Foundation (Required Now)
torch>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
pytest>=7.3.0
safetensors>=0.3.0
psutil>=5.9.0
tokenizers>=0.19.0

# ------------------------------------------------------------------------------
# Future Phase Dependencies (Required in Later Phases - DO NOT INSTALL YET)
# ------------------------------------------------------------------------------
# Phase 8 (Pretraining & Dist): accelerate, datasets, triton, flash-attn
# Phase 9-11 (Instruction & RL): trl, peft, bitsandbytes
# Phase 13 (Legal RAG): faiss-cpu or faiss-gpu, chromadb, rank-bm25
# Phase 14 (Evaluation): lm-eval, rouge-score, bert-score
# Phase 15 (Inference): vllm, fastapi, uvicorn
